//! Compatible category providers (empty — these are dynamic prefix-based providers).
//! Compatible providers use ID prefixes: openai-compatible-*, anthropic-compatible-*

use super::*;

pub static ENTRIES: &[ProviderRegistryEntry] = &[];
