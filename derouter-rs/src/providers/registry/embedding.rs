//! Embedding category providers (empty — embedding providers are in apikey/freeTier by their category).
//! Entries with embedding serviceKinds are classified via service_kinds field.

use super::*;

pub static ENTRIES: &[ProviderRegistryEntry] = &[];
