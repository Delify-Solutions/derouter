//! Proxy responses — /v1/responses handled by chat handler. Phase 1.
//! The /v1/responses endpoint uses the same handleChat flow as /v1/chat/completions.
//! The chat handler detects the request format and delegates appropriately.
