## 1. Frontend TS conversion — shared modules

- [ ] 1.1 Convert `src/shared/components/**` (~37 files) from `.js` to `.tsx` with typed props (no `any` for component prop shapes). Match the established Phase 1-3 component style (`'use client'`, typed props interface, typed event handlers). Preserve all runtime behavior + JSX/styling.
- [ ] 1.2 Convert `src/shared/hooks/**` (4 files) + `src/shared/utils/**` (16 files) + `src/shared/services/**` (3 files) to `.ts`/`.tsx` with typed function signatures; export explicit types for anything consumed by converted pages.
- [ ] 1.3 Convert `src/shared/constants/**` (12 files) to `.ts` with typed exports (e.g. typed `CLI_TOOLS`/`MITM_TOOLS` arrays so cli-tools components get typed tool objects).
- [ ] 1.4 Delete the old `.js`/`.jsx` files as each is replaced by its `.tsx`/`.ts` sibling (one-for-one rename; update import sites that referenced the JS name).

## 2. Frontend TS conversion — remaining app pages + i18n

- [ ] 2.1 Enumerate remaining `src/app/**` non-api `.js`/`.jsx` files NOT already converted in Phases 1-3 (excluding `src/app/api/**`). Convert each dashboard page, layout, error page, and client component to `.tsx` with typed props/state; switch any raw `fetch('/api/...')` to the typed apiClient against the Rust origin (these are admin routes, now Rust-only).
- [ ] 2.2 Convert `src/i18n/**` (3 files) to `.ts` with typed locale/translation shapes.
- [ ] 2.3 Gate: `npx tsc --noEmit` reports 0 errors across the entire `src/` tree. ← (verify: 0 errors repo-wide, no surviving frontend `.js`/`.jsx` outside an explicit generated/vendored allowlist)

## 3. cli-tools config-writer Rust port

- [ ] 3.1 Add a TOML crate to `derouter-rs/Cargo.toml` if not already present (check transitive deps first); needed for codex (`~/.codex/config.toml`) and any other TOML tools. Add `toml = "0.8"` (or current) if absent.
- [ ] 3.2 Port the codex config-writer into `cli_tools.rs` (or a new `cli_tools/` submodule): `GET /api/cli-tools/codex` reads `~/.codex/config.toml` + binary presence, returns `{installed, config, hasderouter, settingsPath}` with apiKey masked; `POST` parses the TOML, sets `model`/`model_provider="derouter"`/`[model_providers.derouter]` (name/base_url/wire_api="responses"/`[http_headers] Authorization`) + `default_subagent_model`, writes apiKey to `~/.codex/auth.json` (NOT config.toml), round-trips unknown fields; `DELETE` removes the derouter section. Faithful to `src/app/api/cli-tools/codex-settings/route.js`.
- [ ] 3.3 Port the claude config-writer: `~/.claude/settings.json` (`env.ANTHROPIC_BASE_URL` + apiKey) + read mcpServers from `~/.claude.json`. Faithful to `claude-settings/route.js`.
- [ ] 3.4 Port each remaining `{tool}-settings` route's config-writer (copilot, cline, opencode, jcode, kilo, openclaw, hermes, grok-build, deepseek-tui, droid, devin, cowork) — one per tool, matching that tool's native config format/fields. Port `cowork-mcp-registry` (external API proxy to `api.anthropic.com/mcp-registry` with pagination + 1h cache + filtering) and `cowork-mcp-tools`.
- [ ] 3.5 Update `derouter-rs/src/main.rs` route mounts: confirm `GET/POST/DELETE /api/cli-tools/{tool}` + `all-statuses` + `antigravity-mitm` + `cowork-mcp-registry` + `cowork-mcp-tools` all hit the new config-writer handlers (not the snapshot reader). Ensure auth on all except any Node-unguarded passthrough.
- [ ] 3.6 Update the TS cli-tools components (`CodexToolCard.tsx`, `ClaudeToolCard.tsx`, `CopilotToolCard.tsx`, etc.) to call the Rust `/api/cli-tools/{tool}` paths (GET/POST/DELETE) via the typed apiClient instead of the Node `/api/cli-tools/{tool}-settings` paths. Update `CLIToolsPageClient.tsx` + `ToolDetailClient.tsx` accordingly.
- [ ] 3.7 Gate: per-tool config-writer sanity test (for codex + claude at minimum: a fixed input produces a config that round-trips and loads in the real CLI if available); `cargo build --release` + `cargo clippy --release` clean; `npx tsc --noEmit` 0 errors. ← (verify: cli-tools Apply writes a real on-disk config the CLI can load; keys masked in read responses; no call to Node `-settings` paths remains in the TS components)

## 4. Node-backend deletion (scope REFINED after on-disk import audit)

**On-disk audit findings (verified 2026-09-05):**
- `src/store/**` (7 files): KEEP + convert to TS — these are pure-frontend Zustand stores (`notificationStore`, `headerSearchStore`, `themeStore`, `providerStore`, `settingsStore`, `userStore`, `index`) importing only `zustand` + `@/shared/constants/config`. Used by providers page, proxy-pools page, Header, ThemeProvider, DashboardLayout, useTheme. NOT backend. The memory's "delete src/store/** (7)" is INCORRECT — do NOT delete.
- `src/shared/services/initializeApp.ts` + `quotaAutoPing.ts` + `bootstrap.ts` are a **Node server-runtime bootstrap trio** (NOT frontend services despite living in src/shared/): `layout.tsx` imports `@/shared/services/bootstrap` → `initializeApp()` → imports `@/lib/localDb`, `@/lib/tunnel`, `@/mitm/manager`, `@/lib/mitmAliasCache`, `@/lib/mcp/stdioSseBridge` + dynamically imports `quotaAutoPing` (→ `@/lib/localDb`, `@/lib/network/connectionProxy`). In Stack A1, Rust owns tunnels/mitm/mcp, so this Node bootstrap is dead. It must be neutralized BEFORE deleting `@/lib/`+`@/mitm/`.
- `src/instrumentation.js`: Next.js server-runtime convention file — KEEP (convert to `.ts`) if `next.config.*` or the build references it; verify before deciding.

- [ ] 4.1 Convert `src/store/**` (7 Zustand stores) JS → TS with Typed zustand interfaces (typed state + actions; e.g. `interface ProviderStoreState { providers: ProviderConnection[]; loading: boolean; ... }`). Delete old `.js`. Run `npx tsc --noEmit` — 0 errors. (These are frontend; they STAY.)
- [ ] 4.2 Neutralize the Node server-runtime bootstrap: in `src/app/layout.tsx` remove the `import "@/shared/services/bootstrap"` line (the Rust backend now owns tunnels/mitm/mcp; there is nothing for Node to bootstrap). Delete `src/shared/services/bootstrap.ts`, `src/shared/services/initializeApp.ts`, `src/shared/services/quotaAutoPing.ts` (they only orchestrate the now-deleted `@/lib/`+`@/mitm/` backend). If any of the 3 has a non-backend export still used by frontend, keep that export as a no-op stub instead — verify with grep first. Run `npx tsc --noEmit` — 0 errors.
- [ ] 4.3 Grep surviving frontend + `next.config.*` + middleware for any remaining `@/lib/`, `@/sse/`, `@/store/` (should only be the converted zustand stores, now `@/store/*.ts`), `@/mitm/`, and root backend files. Any surviving `@/lib/`/`@/mitm/`/`@/sse/` hit is removed (rewired to the Rust apiClient or deleted with its caller) BEFORE deleting the referenced module. Decide `src/instrumentation.js`: keep+convert to `.ts` if `next.config.*` or Next.js build requires it (verify via `npm run build` with it removed); document the decision.
- [ ] 4.4 Delete `src/app/api/**` (~186 route files). Confirm `npx tsc --noEmit` + `npm run build` still pass (no orphaned import to a deleted route).
- [ ] 4.5 Delete `src/lib/**` (~135 modules), `src/sse/**` (~23 proxy handlers), `src/mitm/**` (17). Do NOT delete `src/store/**` (kept — frontend Zustand). Re-run `npx tsc --noEmit` + `npm run build` + `cargo build --release`.
- [ ] 4.6 Delete verified-bad root backend files (`src/proxy.js`, `src/dashboardGuard.js` — note `proxy.js` imports `dashboardGuard`), `src/consoleLogBuffer.js`, `src/localDb.js`, `src/disabledModelsDb.js`, `src/requestDetailsDb.js`, `src/usageDb.js`, `src/dataDir.js`, `src/providerNormalization.js`, `src/androidAppConfig.js`, etc.) — each only after confirming no surviving import (the 4.2 bootstrap deletion should have removed the main consumers). Keep + convert to `.ts` any root file still imported by the surviving frontend/Next.js server (e.g. `instrumentation.ts`).
- [ ] 4.7 Gate: `npx tsc --noEmit` 0 errors, `npm run build` clean, `cargo build --release` clean. Grep confirms no `/api/*` or `/v1/*` request is served by Node (Next.js `app/api/**` absent); no surviving `@/lib/`/`@/mitm/`/`@/sse/` import remains; `@/store/` imports resolve to the converted `.ts` stores. ← (verify: zero orphaned imports; no Node process serves admin/proxy; tsc + npm build + cargo build all green; @/store/ preserved)

## 5. Askama + htmx/alpine removal (the parallel HTMX UI — live, not dead)

- [ ] 5.1 CONFIRMED on disk: Askama is a live parallel UI (from the archived rust-htmx plan), NOT vestigial. Files: `src/templates.rs` (30+ `#[derive(Template)]`), `src/web/render.rs` (Hx-Request partial renderer), `src/web/routes/public_usage.rs` (`use askama::Template`). Routes in main.rs: `/usage` page, `/usage/key/receipts`, `/usage/key/receipts/detail`, `DELETE /usage/key/history`, `dashboard_page` HTML handler. These HTML routes collide with the Next.js frontend (which also serves `/usage` + `/dashboard`); in the 2-service Stack A1 architecture the browser uses Next.js, so the Rust HTML routes are unused. KEEP the JSON siblings (`/api/usage/key` via `public_usage::key_usage_json`, `DELETE /api/usage/key/history` via `clear_history_json`).
- [ ] 5.2 In `main.rs`: remove the HTML route mounts (`/usage`, `/usage/key/receipts`, `/usage/key/receipts/detail`, `DELETE /usage/key/history`, the `dashboard_page` handler + its mount) and any `mod templates;`/`mod render;` declarations. Leave JSON mounts intact.
- [ ] 5.3 Edit `src/web/routes/public_usage.rs`: drop the Askama `page`/`receipts`/`receipt_detail`/`clear_history` HTML handlers + the `use askama::Template` import; keep `key_usage_json` + `clear_history_json`.
- [ ] 5.4 Delete `src/templates.rs`, `src/web/render.rs`, `derouter-rs/templates/`, `derouter-rs/static/`, and `derouter-rs/build.rs` (verify `build.rs` only has the `rerun-if-changed=templates/` directive first; if it has other directives, edit instead of delete).
- [ ] 5.5 Remove `askama = "0.12"` from `derouter-rs/Cargo.toml` (main + dev-dep sections). Grep `derouter-rs/src/` for `use askama`/`Template`/`askama::` — confirm 0 hits after the above; remove any surviving import.
- [ ] 5.6 Edit `derouter-rs/Dockerfile` to remove the `COPY templates/ templates/` and `COPY static/ static/` lines.
- [ ] 5.7 Gate: `cargo build --release` + `cargo clippy --release` clean; `cargo test byte_verify` still 6/6 (cursor D1 unaffected). Grep confirms no `use askama`/`#[derive(Template)]`/`askama::` anywhere in `derouter-rs/src/`. ← (verify: no Askama reference anywhere; JSON /api/usage/key + DELETE /api/usage/key/history still mounted + working; build/clippy clean; cursor D1 tests still 6/6)

## 6. Docker finalize + end-to-end verify

- [ ] 6.1 Verify `docker-compose.yml` is 2-service (Next.js `derouter` + Rust `derouter-rs`); `NEXT_PUBLIC_API_URL` points to `http://derouter-rs:20128`; the shared `derouter-data` volume mounts `/app/data` on BOTH services; the Rust service does NOT set `HOSTNAME` as bind host (it uses `HOST` defaulting to `0.0.0.0` — per rust-docker-bind-host invariant). Remove any Node-backend env hints.
- [ ] 6.2 `docker compose up` → both services healthy (Rust `/api/health` 200; Next.js responds). Run a curl sweep: `/api/auth/status` returns JSON; `/api/providers` → 401 JSON without cookie, `{"connections":[...]}` with cookie; a CLI-tools `GET /api/cli-tools/codex` returns `{installed, config, hasderouter}` with apiKey masked.
- [ ] 6.3 Real-provider proxy sweep: `POST /v1/chat/completions` with a real key+combo returns a streaming completion; status/headers/usage-logging shape matches Phase 3 (Rust executors already owned this; Node deletion is a no-op for `/v1/*`). Only after this passes is the Node deletion in step 4 considered safe to keep.
- [ ] 6.4 Browser smoke: login → dashboard → providers CRUD → cli-tools Apply (writes real `~/.{tool}/config` via Rust) → usage tabs → proxy completion. `npx tsc --noEmit` 0 errors final; `npm run build` clean; `cargo build --release` + `cargo clippy --release` clean. ← (verify: docker compose up green end-to-end; /v1 and /api behavior identical to Phase 3; tsc/clippy/build all clean)
- [ ] 6.5 osf-verify `phase4-frontend-strict-ts-backend-removal`; resolve CRITICALs (max 3 rounds); archive to `archive/2026-09-05-phase4-frontend-strict-ts-backend-removal/`.
